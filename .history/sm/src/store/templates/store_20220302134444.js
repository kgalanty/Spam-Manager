export default {
    namespaced: true,
    state: () => ({

    }),
    mutations: {
        // setFilters(state, filters) {
        //   state.filters = filters
        //   //  // Vue.set(state, 'chats', chats);
        //   //state.chats = chats
        // }
      },
      actions:
      {
      }
}